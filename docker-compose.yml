version: '3.8'

services:
  api:
    build: ./back
    container_name: fastapi_backend
    ports:
      - "8000:8000"
    depends_on:
      - redis
      - ollama
    environment:
      - REDIS_URL=redis://redis:6379
      - OLLAMA_URL=http://ollama:11434
    volumes:
      - ./back:/app